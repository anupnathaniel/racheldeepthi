export type CommunityInitiative = {
  title: string;
  description: string;
};

// Intentionally empty. Initiatives must not be published until factual,
// photographic, rights, privacy and consent reviews are complete.
export const communityInitiatives: CommunityInitiative[] = [];
